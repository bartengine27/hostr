---
- name: Update apt repo and cache
  apt:
    update_cache: yes
    force_apt_get: yes
    cache_valid_time: 3600        
- name: Install dependencies
  apt:
    pkg:
      - gnupg
      - git
    state: latest
    update_cache: yes